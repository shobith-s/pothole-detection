name: Keep Backend Alive

on:
  schedule:
    - cron: "*/10 * * * *"  # Runs every 10 minutes

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping HF Space backend
        run: curl -s https://shobiths-pothole-backend.hf.space/ || true
